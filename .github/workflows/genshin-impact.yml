name: Genshin Impact Data
on:
  schedule:
    # Runs every hour
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  latest-news:
    name: Latest Genshin Impact News
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: gautamkrishnar/blog-post-workflow@master
        with:
          committer_username: "QubitPi"
          committer_email: "jack20220723@gmail.com"
          commit_message: "Genshin Impact News"
          comment_tag_name: "GENSHIN-IMPACT-LATEST-NEWS"
          max_post_count: "5"
          feed_list: "https://rsshub.app/yuanshen"
